apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${DEPLOY_ENV}-termodom-web-postgres-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - db-${DEPLOY_ENV}.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: db-${DEPLOY_ENV}.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ${DEPLOY_ENV}-termodom-web-postgres-service
            port:
              number: 5432
