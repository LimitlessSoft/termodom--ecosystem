apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-termodom-office-public-api-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - api-office.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: api-office.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: production-termodom-office-public-api-service
            port:
              number: 80
