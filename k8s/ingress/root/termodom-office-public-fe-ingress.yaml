apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-termodom-office-public-fe-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - office.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: office.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: production-termodom-office-public-fe-service
            port:
              number: 80
