apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-termodom-office-mass-sms-api-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - api-office-mass-sms.termodom.rs
      secretName: star-termodom-tls
  rules:
    - host: api-office-mass-sms.termodom.rs
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: production-office-mass-sms-api-service
                port:
                  number: 82
