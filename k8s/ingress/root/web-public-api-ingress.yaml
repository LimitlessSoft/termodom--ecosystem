apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-web-public-api-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - api.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: api.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: production-web-public-api-service
            port:
              number: 80
