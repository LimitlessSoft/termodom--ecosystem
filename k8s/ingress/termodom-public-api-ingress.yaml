apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: termodom-public-api-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - api-stage.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: api-stage.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: termodom-public-api-service
            port:
              number: 80
