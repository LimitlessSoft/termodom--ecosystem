apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: root-termodom-web-admin-api-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - api-admin.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: api-admin.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: production-termodom-web-admin-api-service
            port:
              number: 80
