apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${DEPLOY_ENV}-office-public-fe-ingress
  namespace: termodom-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
  - hosts:
    - office-${DEPLOY_ENV}.termodom.rs
    secretName: star-termodom-tls
  rules:
  - host: office-${DEPLOY_ENV}.termodom.rs
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ${DEPLOY_ENV}-office-public-fe-service
            port:
              number: 80
