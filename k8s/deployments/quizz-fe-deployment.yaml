apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${DEPLOY_ENV}-quizz-fe-deployment
  namespace: termodom-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${DEPLOY_ENV}-quizz-fe
  template:
    metadata:
      labels:
        app: ${DEPLOY_ENV}-quizz-fe
    spec:
      containers:
      - name: ${DEPLOY_ENV}-quizz-fe
        image: limitlesssoft/termodom-quizz-fe:${DEPLOY_ENV}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
      imagePullSecrets:
      - name: regcred