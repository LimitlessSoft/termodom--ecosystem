apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${DEPLOY_ENV}-termodom-office-public-fe-deployment
  namespace: termodom-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${DEPLOY_ENV}-termodom-office-public-fe
  template:
    metadata:
      labels:
        app: ${DEPLOY_ENV}-termodom-office-public-fe
    spec:
      containers:
      - name: ${DEPLOY_ENV}-termodom-office-public-fe
        image: limitlesssoft/termodom-office-public-fe:${DEPLOY_ENV}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
      imagePullSecrets:
      - name: regcred