apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-office-public-fe-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-office-public-fe
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: NodePort