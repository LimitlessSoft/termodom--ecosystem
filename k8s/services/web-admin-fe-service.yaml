apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-web-admin-fe-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-web-admin-fe
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: NodePort