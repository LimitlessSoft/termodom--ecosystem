apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-trace-logs-api-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-trace-logs-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: NodePort