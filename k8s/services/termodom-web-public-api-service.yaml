apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-termodom-web-public-api-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-termodom-web-public-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: NodePort