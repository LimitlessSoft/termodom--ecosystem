apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-termodom-public-api-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-termodom-public-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort