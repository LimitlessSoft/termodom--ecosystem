apiVersion: v1
kind: Service
metadata:
  name: ${DEPLOY_ENV}-termodom-web-admin-api-service
  namespace: termodom-namespace
spec:
  selector:
    app: ${DEPLOY_ENV}-termodom-web-admin-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: NodePort