name: Release stage termodom-web-public-api

on:
  workflow_call:
  push:
    branches: [ "stage" ]
    
jobs:
  deploy:
    uses: limitlesssoft/termodom--ecosystem/.github/workflows/build-and-push-termodom-web-public-api.yml@stage
    secrets: inherit
    with:
      env: stage
