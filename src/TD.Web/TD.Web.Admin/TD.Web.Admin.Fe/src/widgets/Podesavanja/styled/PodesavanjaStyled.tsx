import { Box, styled } from '@mui/material'

export const PodesavanjaStyled = styled(Box)(
    ({ theme }) => `
        z-index: -1;
        height: 100vh;
        width: 100%;
        overflow: hidden;
    `
)
