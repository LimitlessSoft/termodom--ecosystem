# Use the official Node.js image as a base image
FROM node:22-bookworm

RUN apt update
RUN apt install -y postgresql-client

WORKDIR /app

COPY . .

RUN npm install

CMD [ "pg_dump -U postgres -h 192.168.0.17 release_tdoffice > ~/asd.txt" ]
